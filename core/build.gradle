apply plugin: "java"
apply plugin: "kotlin"

sourceCompatibility = jdk_compatibility
targetCompatibility = jdk_compatibility

dependencies {
    compile "org.jetbrains.kotlin:kotlin-stdlib-jre7:$kotlin_version"
}
